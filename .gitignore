# Python
__pycache__/
*.py[cod]
*$py.class
*.so
*.egg-info/
dist/
build/
.eggs/
*.egg

# Virtual environments
.venv/
venv/
env/

# IDE & tooling caches
.idea/
.vscode/
.claude/
*.swp
*.swo

# Scratch notebooks at repo root (not part of the pipeline)
/Gemma4_*.ipynb
/gemma4-*.ipynb

# Generated outputs
kb/output/*.sqlite
datagen/output/*.jsonl
datagen/output/pill_images/*.png
training/checkpoints/
rl/checkpoints/
export/output/*.task
eval/reports/*.md
eval/reports/*.json

# OS
.DS_Store
Thumbs.db

# Secrets
.env
*.key

# Android
android/.gradle/
android/.kotlin/
android/build/
android/app/build/
android/local.properties
android/app/src/main/assets/aegis_model.task
android/app/src/main/assets/aegis_kb.sqlite

# Merged model checkpoints (too large for git — track via HF Hub or cloud bucket)
training/checkpoints/**/aegis-sft-merged/
rl/checkpoints/**/aegis-grpo-merged/

# Node
node_modules/
demo/frontend/dist/
demo/frontend/.next/

# Model weights (too large for git)
*.bin
*.safetensors
*.gguf
*.tflite
*.litertlm
*.task
models/
kb/output/*.bak
kb/output/*.sqlite-shm
kb/output/*.sqlite-wal

# Jupyter
.ipynb_checkpoints/

# W&B
wandb/

# Local model downloads (.litertlm covered above; this also catches
# extracted sections, community/aegis subdirs, configs, README)
downloads/

# On-device debug captures
*-logcat*.txt
*-logcat.utf8.txt
*.tombstone
tombstones/
crash.txt
tensor-diff.txt
android/*-logcat*.txt
android/tombstones/

# Splice/extraction intermediates
*.pbtext
*.zlib
*.spiece
*.toml.bak

# Stale session docs (handovers / plans / step trackers).
# ON-DEVICE-DEPLOYMENT-ANALYSIS.md is intentionally NOT ignored — it's
# the current canonical doc on why the app ships LiteRT-LM CPU.
HANDOVER.md
HANDOVER-LITERTLM-DEBUG.md
PLAN.md
remaining_steps.md

#Recordings
recordings/
eval/output/

# GSD planning artifacts (codebase maps, plans — local only)
.planning/

# Eval kit build artifact (reproducible via `python build_eval_kit.py`)
aegis_eval_kit.zip
C--Users-amanr.claude-settings.json.url
