# Tata 1mg Labs Lab Report Fixture (Phase 4.1)

**Fixture:** tata1mg_cbc.pdf
**Panel type:** CBC + A1c + fasting glucose
**Slot:** tata1mg (VendorRegistry slot 0 — brand-token, R-02)
**Status:** Synthetic (D-12)
**PHI status:** ZERO PHI — fake patient identity only

## Provenance

- **Source:** synthetic — generated by `tools/parsers/synthesize_fixture.py::generate_tata1mg`
- **Generator version:** reportlab>=4.0,<5.0 (canvas API, `invariant=1`)
- **Retrieval date:** 2026-05-15 (Plan 04.1-5-01, Wave 5)
- **License observation:** N/A — fully synthetic, written for this repo
- **Redistribution rationale:** no real customer PDF is ever committed per D-12; the user's real Tata 1mg PDF (filename hash `1e91d2969f…` per 04.1-2-01-NOTES.md) stays user-local
- **SHA256:** 781bb291c6b9cd69fd27c74bd36556b410bee9e9c9ad44302398d8c1e2b6ab0e
- **Re-generate command:** `python tools/parsers/synthesize_fixture.py`
- **Real customer PDFs:** NEVER committed (D-12)

## Fake Patient Data (zero PHI)

- Name: Test Patient
- DOB: 1990-01-01
- Sex: M
- MRN: TEST-001
- Report Date: 2026-05-14

## Expected Fingerprint Behavior

Page-1 lower-cased text contains all 3 Tata1mgExtractor brand-token substrings sourced from 04.1-2-01-NOTES.md empirical inspection:

- `tata 1mg | labs`            (masthead)
- `personal health smart report` (large title)
- `tata 1mg app`               (promo footer — defence-in-depth fingerprint)

`VendorRegistry.fingerprintMatches(page1)` therefore selects `Tata1mgExtractor` (slot 0) before falling through to keyword-fingerprint extractors or the GenericExtractor catch-all (slot 7).

## Source for the Row Set

Body rows are the verbatim Family-B sample rows captured in `04.1-2-01-NOTES.md` (CBC + A1c + fasting glucose subset). Values are clinically plausible but invented; no real patient values appear.

Family-B column order: `TEST_NAME  VALUE  UNIT  REF_LOW - REF_HIGH`. The fixture includes the `ÂµL` mojibake variant (`10^3/ÂµL`) so Tata1mgExtractor's mojibake-aware UNITS_TOKEN alternation is exercised.

## Test Contract

Field-level assertions per Phase 4.1 D-15 (NOT byte-identical GT JSON — that contract is reserved for the 5 existing Phase 2 ported vendors per D-11):

- `vendorKey == "tata1mg"`
- `report_status.code == "OK"`
- `canonical_name` populated for every emitted row (alias-map silent-drop on miss)
- `value` parseable as int|float|null per LM-3
- `status ∈ {IN_RANGE, BORDERLINE, OUTSIDE_RANGE, unknown}`
- `defer_reason` from Phase 2 D-12 vocabulary when `status == unknown`, else null
- `rows.size >= expected_min` (specific expected_min landed by Wave 7 / Plan 04.1-5-02 androidTest)

## Determinism

reportlab `canvas.Canvas(..., invariant=1)` plus pinned document metadata makes `python tools/parsers/synthesize_fixture.py` produce a byte-identical PDF across reruns (locally verified on reportlab 4.5.1). If a future reportlab upgrade breaks invariant=1 byte-stability, document the new SHA256 here and update the synthesize_fixture.py docstring.

---

*Manifest created: 2026-05-15 as part of Phase 4.1 Wave 5 (Plan 04.1-5-01).*
