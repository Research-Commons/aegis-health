# Acme Diagnostics Lab Report Fixture (Phase 4.1)

**Fixture:** acme_diagnostics_cbc.pdf
**Panel type:** generic chemistry (Sodium / Chloride / Calcium / Albumin / Glucose)
**Slot:** generic (VendorRegistry slot 7 — catch-all, R-02 last)
**Status:** Synthetic (D-14 — empirical proof that the generic-fallback path activates)
**PHI status:** ZERO PHI — fake patient identity only

## Provenance

- **Source:** synthetic — generated by `tools/parsers/synthesize_fixture.py::generate_acme`
- **Generator version:** reportlab>=4.0,<5.0 (canvas API, `invariant=1`)
- **Retrieval date:** 2026-05-15 (Plan 04.1-5-01, Wave 5)
- **License observation:** N/A — fully synthetic, written for this repo
- **Redistribution rationale:** D-14 mandates a fully-invented PDF (Acme Diagnostics Inc. is a fictitious vendor) so the anti-fingerprint pytest can assert empirical exclusion of the 15 named-vendor fingerprint substrings
- **SHA256:** 0e166c88930d1c709a850326b57851103cd29efa871ef1bd289a097379dca8a5
- **Re-generate command:** `python tools/parsers/synthesize_fixture.py`
- **Real customer PDFs:** NEVER committed (D-14)

## Fake Patient Data (zero PHI)

- Name: Test Patient
- DOB: 1990-01-01
- Sex: M
- MRN: TEST-003
- Report Date: 2026-05-14

## Expected Fingerprint Behavior

Page-1 lower-cased text contains NONE of the 15 named-vendor fingerprint substrings (enforced by `eval/tests/test_acme_diagnostics_anti_fingerprint.py`):

  labcorp · lipid panel · comprehensive metabolic panel · complete blood count ·
  haematology · hematology · lipid profile · biological ref · hgb a1c ·
  tata 1mg · 1mg labs · 1mg health ·
  dr lal pathlabs · dr lal path labs · drlpl

Therefore all 7 named extractors' `fingerprintMatches` return false, and `VendorRegistry.fingerprintMatches(page1)`'s `firstOrNull` semantics fall through to `GenericExtractor` at slot 7 (D-03 unconditional `true`). The `ReportReaderPipeline.parse` aggregate-floor gate (D-02 `≥ 3` post-normalization rows) is cleared by the 5 chemistry rows, so the pipeline emits `report_status.code == "GENERIC_FALLBACK"` (NOT `UNKNOWN_VENDOR`).

Notably AVOIDED phrasing on page 1:

- "Hematology" / "Haematology"      (Mayo fingerprint)
- "Comprehensive Metabolic Panel"   (Quest fingerprint)
- "Complete Blood Count"            (Mayo fingerprint)
- "Lipid Panel" / "Lipid Profile"   (LabCorp / HospitalLis fingerprints)
- "Biological Ref"                  (HospitalLis fingerprint — header uses "Reference Interval" instead)
- "Hgb A1c"                         (UrgentCare fingerprint)
- "Tata 1mg" / "1mg Labs" / "1mg Health"  (Tata1mg fingerprints)
- "Dr Lal PathLabs" / "Dr Lal Path Labs" / "DRLPL"  (DrLal fingerprints)
- "LabCorp"                         (LabCorp fingerprint)

## Source for the Row Set

Body rows are 5 generic chemistry analytes in Family-B column order (`TEST_NAME  VALUE  UNIT  REF_LOW - REF_HIGH`). The row count (5) clears the aggregate-floor gate (≥ 3 required); every row carries both units AND a bilateral range so each passes the per-row gate (`hasUnits OR hasRange`).

Values invented; clinically plausible.

## Test Contract

SMOKE assertions per Phase 4.1 D-14 / D-15 (NOT field-level — Acme is the generic-fallback proof, not a deeply-curated fixture):

- `vendorKey == "generic"`
- `report_status.code == "GENERIC_FALLBACK"`
- `rows.size >= 3` (aggregate-floor cleared)
- Per-row gate enforced: every emitted row has `hasUnits OR hasRange == true`
- Anti-fingerprint invariant (CI gate): `eval/tests/test_acme_diagnostics_anti_fingerprint.py` passes — page-1 lower-cased text contains NONE of the 15 named-vendor substrings

The end-to-end pipeline assertion (vendorKey + report_status emission) lands in the Wave 7 androidTest (`04.1-5-02-PLAN`); the pytest in this plan covers the page-1 anti-fingerprint property in isolation, avoiding duplication of the Kotlin pipeline.

## Determinism

reportlab `canvas.Canvas(..., invariant=1)` plus pinned document metadata makes `python tools/parsers/synthesize_fixture.py` produce a byte-identical PDF across reruns (locally verified on reportlab 4.5.1). If a future reportlab upgrade breaks invariant=1 byte-stability, document the new SHA256 here and update the synthesize_fixture.py docstring.

---

*Manifest created: 2026-05-15 as part of Phase 4.1 Wave 5 (Plan 04.1-5-01).*
