# Dr Lal PathLabs Lab Report Fixture (Phase 4.1)

**Fixture:** drlalpathlabs_lipid.pdf
**Panel type:** Liver & Kidney panel (NABL Swasthfit body) + defensive lipid rows
**Slot:** drlalpathlabs (VendorRegistry slot 1 — brand-token, R-02)
**Status:** Synthetic (D-13 — Apollo fallback NOT triggered, public Dr Lal sample acquired)
**PHI status:** ZERO PHI — fake patient identity only

## Provenance

- **Source:** synthetic — generated by `tools/parsers/synthesize_fixture.py::generate_drlalpathlabs`
- **Generator version:** reportlab>=4.0,<5.0 (canvas API, `invariant=1`)
- **Retrieval date:** 2026-05-15 (Plan 04.1-5-01, Wave 5)
- **License observation:** N/A — fully synthetic, written for this repo
- **Redistribution rationale:** the publicly-distributed Dr Lal sample PDF inspected during Wave 2 (`drlal-sample-1.pdf`, filename hash `df84695bf8…` per 04.1-2-01-NOTES.md) carried no explicit redistribution license and is NOT committed; this synthetic file is the repo-safe substitute
- **SHA256:** 6819beb5b9802ba429d1d98cf31ee00e52404c62416b9413930eb69069b9df39
- **Re-generate command:** `python tools/parsers/synthesize_fixture.py`
- **Real customer PDFs:** NEVER committed (D-13)

## Fake Patient Data (zero PHI)

- Name: Test Patient
- DOB: 1990-01-01
- Sex: F
- MRN: TEST-002
- Report Date: 2026-05-14

## Expected Fingerprint Behavior

Page-1 lower-cased text contains 2 DrLalPathLabsExtractor brand-token substrings sourced from 04.1-2-01-NOTES.md empirical inspection:

- `dr lal pathlabs`              (masthead)
- `lpl-national reference lab`   (processed-at section)

`VendorRegistry.fingerprintMatches(page1)` therefore selects `DrLalPathLabsExtractor` (slot 1) — Tata1mgExtractor at slot 0 rejects (none of its 6 fingerprints match), brand-token-first R-02 ordering keeps Mayo's permissive `"hematology"` keyword from claiming this PDF.

## Source for the Row Set

Body rows are verbatim Family-B sample rows from `04.1-2-01-NOTES.md` (Liver & Kidney panel) plus three intentional parenthetical method lines (`(Modified Jaffe,Kinetic)`, `(Urease UV)`, `(IFCC without P5P)`) so DrLalPathLabsExtractor's panel-heading-skip / parenthetical-method-skip logic is exercised. The GFR row carries the inequality range `>59` and the `mL/min/1.73m2` unit token — both NOTES.md-documented Dr Lal divergences from the generic Family-B shape.

## Test Contract

Field-level assertions per Phase 4.1 D-15 (NOT byte-identical GT JSON per D-11):

- `vendorKey == "drlalpathlabs"`
- `report_status.code == "OK"`
- `canonical_name` populated for every emitted row (alias-map silent-drop on miss)
- `value` parseable as int|float|null per LM-3
- `status ∈ {IN_RANGE, BORDERLINE, OUTSIDE_RANGE, unknown}`
- `defer_reason` from Phase 2 D-12 vocabulary when `status == unknown`, else null
- `rows.size >= expected_min` (specific expected_min landed by Wave 7 / Plan 04.1-5-02 androidTest)
- Method lines (parenthetical-only, e.g. `(Urease UV)`) MUST NOT produce ParsedRow entries

## Determinism

reportlab `canvas.Canvas(..., invariant=1)` plus pinned document metadata makes `python tools/parsers/synthesize_fixture.py` produce a byte-identical PDF across reruns (locally verified on reportlab 4.5.1). If a future reportlab upgrade breaks invariant=1 byte-stability, document the new SHA256 here and update the synthesize_fixture.py docstring.

---

*Manifest created: 2026-05-15 as part of Phase 4.1 Wave 5 (Plan 04.1-5-01).*
